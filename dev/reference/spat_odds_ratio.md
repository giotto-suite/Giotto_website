# Spatial odds-ratio

calculate odds-ratio

## Usage

``` r
.spat_or_func(feat, bin_matrix, spat_mat, calc_hub = FALSE, hub_min_int = 3)

.spat_or_func_dt(
  bin_matrix_DTm,
  spat_netw_min,
  calc_hub = FALSE,
  hub_min_int = 3,
  cores = NA
)
```

## Value

numeric

## Functions

- `.spat_or_func_dt()`: data.table implementation
